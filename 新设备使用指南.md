# 新设备使用指南：Obsidian + GitHub + Zotero 科研库

> 适用：在一台**全新的电脑**上，从零把本科研知识库（obsidian-sync）跑起来。
> 本指南融合了两套可互通的导入方式：**方案 A（墨问资料主推）＝ Zotero 剪藏 + Mdnotes + Better BibTeX + Obsidian Citations**；**方案 B（本库已内置）＝ obsidian-zotero-desktop-connector 一键 Import notes**。日常任选其一，落点都是 `07_Literature/<citekey>.md`，共享同一套 `[[citekey]]` 溯源规则。
> 资料出处：维客笔记《Zotero 与 Obsidian 交互 助力科研》(墨问笔记 f1xbfq0sEGmtqc7YBDUb3 / B 站专栏 cv14316452)。

## 0. 先理解"谁负责什么"

| 工具 | 角色 | 装在哪 | 必装? |
| --- | --- | --- | --- |
| Obsidian | 笔记与双链知识网 | 本机 | ✅ |
| GitHub | 私有云同步(obsidian-sync) | 云端 | ✅ |
| obsidian-git(Obsidian 插件) | 每 5 分钟自动 commit+push | Obsidian | ✅ |
| Zotero | 文献库 + PDF 阅读批注 | 本机 | ✅ |
| Better BibTeX(Zotero 插件) | 生成稳定 citekey、导出 .bib | Zotero | ✅ |
| Mdnotes(Zotero 插件) | 把题录/笔记按模板导出 Markdown 进库(方案 A) | Zotero | 方案 A 用 |
| obsidian-zotero-desktop-connector(Obsidian 插件) | Zotero→Obsidian 一键导入(方案 B) | Obsidian | 方案 B 用 |
| Citations(Obsidian 插件) | 在笔记里按 citekey 插入/引用文献 | Obsidian | 可选(方案 A 用) |

## 1. 准备阶段（一次性）

### 1.1 系统依赖
- 安装 [Git for Windows](https://git-scm.com/)（一路默认即可，它会带 Git Credential Manager）；
- 安装 [Obsidian](https://obsidian.md/)；
- 安装 [Zotero 7](https://www.zotero.org/download)。

### 1.2 拉到本仓库（等于拿到整个知识库骨架）
在终端执行（GitHub 账号需能访问私有仓库 obsidian-sync）：

```bash
git clone https://github.com/hcming2026/obsidian-sync.git
cd obsidian-sync
```

### 1.3 用 Obsidian 打开
Obsidian → "Open folder as vault" → 选择刚 clone 的 `obsidian-sync` 目录。

## 2. Obsidian 端配置

### 2.1 安装社区插件
设置 → 第三方插件 → 关闭"安全模式" → 浏览安装：

| 插件 | 用途 | 必装 |
| --- | --- | --- |
| **Obsidian Git** | 自动同步 GitHub（配置已随仓库共享） | ✅ |
| **obsidian-zotero-desktop-connector** | 方案 B 一键导入（配置已共享，无需重设） | 方案 B |
| **Citations** | 方案 A：按 `.bib` 引用文献 | 方案 A |

装完 Obsidian Git 后它会读取仓库里共享的 `data.json`（自动备份间隔 5 分钟、push 前先 pull），一般无需再设置；首次请执行一次命令 `Obsidian Git: Pull` 确认连通。

### 2.2 确认模板目录（方案 A 需要）
模板统一放 `05_Templates/`。需要在该目录放一个文件名**必须叫** `Mdnotes Default Template` 的笔记，作为 Zotero 导出的排版模板，参考内容：

```markdown
# {{title}}

> 引用键：`{{citekey}}` ｜ 作者：{{creators}} ｜ 年份：{{date}}
> Zotero 链接：{{zoteroSelectURI}}

## 摘要
{{abstractNote}}

## 标签
{{tags}}

## 我的笔记
（在 Obsidian 里直接补充精读心得；建议用 `[[文件名]]` 链向相关原子笔记）

## 元信息
- 期刊/出处：{{publicationTitle}}
- DOI：{{DOI}}
- URL：{{url}}
```

> 注：不同 Mdnotes 版本字段写法略有差异；以你安装版本「Mdnotes Preferences → 打开模板」生成的官方默认模板为准，上表只是结构示意。**不要手动改导出的笔记命名**——Mdnotes 会用 citekey 命名，正好匹配库内 `07_Literature/<citekey>.md` 约定。

## 3. Zotero 端配置

### 3.1 安装 Zotero 插件
Zotero → 工具 → 插件(Add-ons) → 齿轮 → Install from file，依次安装 `.xpi`：
1. **Better BibTeX**（官网 https://retorque.re/zotero-better-bibtex/）；
2. **Mdnotes for Zotero**（GitHub 下载 .xpi），装完重启 Zotero。

### 3.2 Better BibTeX 设置（可选但推荐）
- 工具 → Better BibTeX 偏好 → Citation key：用默认 `[auth:lower][year]` 即可，保持稳定，改动会破坏既有 `[[citekey]]` 链接；
- 建议开启 **自动导出**：把整库导出为一个 `library.bib` 到 `obsidian-sync/07_Literature/library.bib`，供 Obsidian Citations 读取。

### 3.3 Mdnotes 偏好设置（核心）
Zotero → 工具 → **Mdnotes Preferences**，按下表填：

| 设置项 | 填写值 | 说明 |
| --- | --- | --- |
| File organization | 按你喜好（如 Split files / 单文件） | 决定附注是否分文件 |
| Export directory（导出目录） | `…/obsidian-sync/07_Literature/` | 让导出的 Markdown 直接进库 |
| Template folder（模板目录） | `…/obsidian-sync/05_Templates/` | 指向我们在 Obsidian 建的模板文件夹 |
| Default template | `Mdnotes Default Template` | 见 2.2 |

### 3.4 初次联动：Zotero 剪藏
浏览器装 Zotero Connector，看到论文网页点一下 → 题录 + PDF 自动进 Zotero（这就是"剪藏"）。

## 4. 日常使用（五步）

1. **收**：网页/DOI 剪藏进 Zotero，在 Zotero 里读 PDF、高亮、写批注；
2. **导**：选中文献 → 右键 **Mdnotes → Create Mdnotes**（方案 A）或保持 Zotero 打开后在 Obsidian 执行 `Import notes`（方案 B）→ 生成 `07_Literature/<citekey>.md`；
3. **读**：打开该笔记，整理摘要与自己的理解，把 `{{…}}` 占位符没填上的字段补好；
4. **拆**：把可复用的方法/结论拆成原子笔记写入 `03_Knowledge/` 对应领域，正文用 `[[citekey]]` 溯源；同类笔记满 5 篇可让 Claude Code 跑 `/process-literature` 建 MOC；
5. **同步**：Obsidian 开着即每 5 分钟自动 push；想立即上传就 `Ctrl+P` → `Obsidian Git: Push`。

## 5. 新设备校验清单（首次跑通后逐项勾）

- [ ] `git pull` 无冲突，Obsidian 打开后能见全部目录与笔记
- [ ] Obsidian Git 状态栏显示已同步（能 Push/Pull）
- [ ] Zotero 中 Better BibTeX 生成的 citekey 与库内已有链接一致
- [ ] 从 Zotero 对任一文献执行 Mdnotes → 在 `07_Literature/` 生成 `<citekey>.md`
- [ ] GitHub 网页上能看到该笔记（证明整条链路闭环）

## 6. 常见问题

| 现象 | 处理 |
| --- | --- |
| 推送提示要登录 / 401 | 先在本机任意终端 `git push` 完成一次 GitHub 登录（HTTPS + 凭据管理器） |
| Mdnotes 找不到模板 | 检查 Mdnotes Preferences 的 Template folder 与模板文件名是否完全一致 |
| 两套方案同时导同一篇文献会覆盖 | 同一篇只用一种导入方式；已在库中的以 Mdnotes 或 ZDC 任一为准 |
| 误改 citekey | Better BibTeX 中恢复旧键或手动改回，避免链接失效 |
| PDF 太大/版权敏感 | PDF 只放 Zotero（本机/官方同步），不进 GitHub 仓库 |

---
*配套规则见 `CLAUDE.md`（职责六：文献处理）、`README.md`（同步实操）。*
